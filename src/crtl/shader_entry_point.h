#pragma once

namespace crtl {
class ShaderEntryPoint {
    // TODO: Should have the name and ref to compiled parameter binding info
    // along with parameter size, etc. so we can set params up
};
}
